# The following libraries are standard and come pre-installed with Python:
# os, time, json, threading, sys
# tkinter (GUI library, included with most Python distributions)
# tkinter.font and tkinter.scrolledtext (also part of tkinter)

psutil==5.9.6
requests==2.31.0
